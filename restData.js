module.exports = function() {
  // Definición de un objeto `data` que contiene un arreglo de superhéroes
  var data = {
    superheroes: [{
      id: 1,
      name: "spiderman",
      Comic: "Marvel"
    }]
  };

  // Devuelve el objeto `data`
  return data;
}
